# 05 — Math engine

## Final score

```
S = 0.50 * A + 0.30 * R + 0.20 * T + B
```

## Academic A

Slots **40/30/20/10**. Multiplier from seed weights → ×0.6–×1.8.

Missing seed weight for a graded subject → **specialty average mapped multiplier** (not 0.75).

No stream μ.

## RIASEC R

`0.3 * cosine + 0.7 * codeMatch` on top-3.

## Technical T

`0.45 * streamBase + 0.55 * marksFit`.

## Génie B

Tech Math only, +2…+8 on matching codes.
