# 12 — Phase B summary

**Status:** Merged to `main`. B0 + B1 shipped.

DELETE `/api/v1/analytics/students/:id` requires `X-Admin-Token` matching server `ADMIN_TOKEN` (mandatory in production; if unset in local dev only, delete remains open).

B2 (logins, retention, capacity) still out of scope.
