# 15 — Stress scan (runtime + build verification)

External runtime scan (clone + install + tsc/build + fresh SQLite boot), 2026-08-16.

## Findings (original)

| ID | Severity | Item |
|----|----------|------|
| 1 | P0 | Server crashes on fresh DB — top-level `db.prepare` in `persist.ts` before `createTables()` |
| 2 | P0 | Client `npm run build` fails — `details: Record<string, unknown>` vs `Step3Results` field access |
| 3 | P1 | Server `tsc` strict-mode gaps (~13) — **not fixed in this pass** |

Report 13 analytics issues confirmed fixed at scan time.

## Fixes applied (same day)

### 1. `server/src/db/persist.ts`
Prepared statements are **lazy** (`getStatements()` on first `persistEvaluation` call), after `initDatabase()` has created tables.

### 2. `client/src/types.ts`
Added `MatchDetails` interface matching `engine.ts` payload; `SpecialtyMatchBreakdown.details` is now `MatchDetails` instead of `Record<string, unknown>`.

## Still open

- Server `tsc` exit non-zero under `noUncheckedIndexedAccess` / `exactOptionalPropertyTypes` (P1)
- Re-run full smoke checklist on a **fresh** clone after pull
