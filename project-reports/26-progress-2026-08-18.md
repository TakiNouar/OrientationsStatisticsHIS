# 26 — Progress log (2026-08-16 → 2026-08-18)

## Engine

- Preference weight **0.05**; RIASEC reduced to **0.25**
- Required `preferredSpecialtyCode` in API schema + Step 1
- Soft preference scores 100 / 50
- **Specialty-dependent academic slots (A/2)**: polarity flip when stream type ≠ specialty type

## Google Sheets

- Layout **A–P** (16 columns): preferred specialty + top-3 code/score/label
- Replaced append with **full resync** from SQLite
- Triggers: after persist, after admin delete, every **5 minutes**
- Idempotent styling: delete bands → clear body formats → reapply header/zebra/percent/highlights
- Brass G–H when preferred matches Specialty #1
- Service account must be **Editor** on the spreadsheet

## Database

- Column `students.technical_option` (+ safe `ALTER`)
- Persist + `listAllSessionsForSheet` export real génie option

## Frontend

- Step 1 / Step 2 restyled to brass/ink (aligned with shell & results)

## Explicit non-goals (unchanged)

- No Docker, no Postgres, no Phase B2 logins
- No automated test suite
- Sheet never rebuilds/wipes SQLite
